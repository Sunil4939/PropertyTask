import theme, { COLORS, SIZES, FONTS, SECONDARY_FONTS, darkTheme, lightTheme, selectedTheme } from "./theme";
import images from "./images";
import icons from "./icons";
import data from "./data";

export {
   images,
   icons,
   data,
   theme,
   FONTS,
   SECONDARY_FONTS,
   SIZES,
   COLORS,
   darkTheme,
   lightTheme,
   selectedTheme
}
